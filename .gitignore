*.pdb
*.obj
*.bin
/bin/
/obj/
/.vs/
/x64/
/x86/
.DS_Store
Thumbs.db
*.user
*.suo

# Build tooling and output — always installed/built fresh, never committed.
# (node_modules previously committed here silently lost every .exe/.dll to
# the *.exe/*.dll ignore rules above, which is what broke the Windows CI
# build: electron-builder's own bundled 7za.exe was missing on disk.)
node_modules/
dist/
server.bundle.cjs
